syntax = "proto3";

package ur.gh;

import "core.proto";

// GitHub CLI proxy service — transparent command passthrough with streaming output
service GhService {
  rpc Exec(GhExecRequest) returns (stream ur.core.CommandOutput);
}

message GhExecRequest {
  repeated string args = 1;
}
