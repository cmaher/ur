name: CI

on:
  pull_request:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install cargo-make
        run: cargo install cargo-make

      - name: Run CI checks
        run: cargo make ci

  # acceptance: disabled until s1-mzgh is resolved (needs local testing with limactl containerd)
  # acceptance:
  #   runs-on: ubuntu-latest
  #   needs: ci
  #   steps:
  #     - uses: actions/checkout@v5
  #     - name: Install Rust toolchain
  #       uses: actions-rust-lang/setup-rust-toolchain@v1
  #     - name: Install cargo-make
  #       run: cargo install cargo-make
  #     - name: Build workspace (includes agent_tools, native on Linux)
  #       run: cargo build --workspace
  #     - name: Copy agent_tools into container build context
  #       run: cp target/debug/agent_tools containers/claude-worker/agent_tools
  #     - name: Build container image
  #       run: docker build -t ur-worker:latest containers/claude-worker
  #     - name: Run acceptance tests
  #       run: cargo test -p acceptance --features acceptance
  #       env:
  #         UR_CONTAINER: docker
