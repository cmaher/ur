name: Setup workspace
description: Install all project tools via mise

runs:
  using: composite
  steps:
    - name: Install tools via mise
      uses: jdx/mise-action@v3

    - name: Cache Rust build artifacts
      uses: Swatinem/rust-cache@v2
