/// String constants for `agent_type` values.
///
/// Use these instead of string literals when constructing gRPC requests or
/// comparing `agent_type` fields on proto types.
pub const CLAUDE: &str = "claude";
