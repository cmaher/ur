# ur_common (Shared Constants)

Zero-dependency crate providing shared constants used across the workspace. Kept intentionally lightweight so any crate can depend on it without pulling in heavy dependencies.

- `lifecycle` module: string constants for ticket lifecycle statuses
