# Tickets (tracked on orphan 'tickets' branch via git worktree)
.tickets
.worktrees

# Build artifacts
target/
containers/claude-worker/bin/
containers/server/ur-server
containers/server/builderd
containers/server/libonnxruntime.so

# Claude Code local skills
.claude/skills/

# Local cargo config (sccache, etc.)
.cargo/config.toml

# Bacon
.bacon-locations
.source_*
